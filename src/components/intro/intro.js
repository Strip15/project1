import './intro.scss';

const IntroList = () => {
    const IntroListElement = document.createElement('div')
    IntroListElement.className = 'intro'

    return IntroListElement
};

export default IntroList;
